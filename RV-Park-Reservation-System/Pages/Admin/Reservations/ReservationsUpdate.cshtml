@page
@model RV_Park_Reservation_System.Pages.Admin.Reservations.ReservationsUpdateModel
@{
}

<br />
<br />
<div class="container">
    <form class="form" method="post">
        <div class="row">
            <input type="hidden" value="@Model.CustomerReservation.ResID" asp-for="@Model.CustomerReservation.ResID" />
            <div class="form-group col-md-6">
                <label asp-for="@Model.CustomerReservation.ResID">Reservation #</label>
                <input class="form-control" asp-for="@Model.CustomerReservation.ResID" value="@Model.CustomerReservation.ResID" readonly>
            </div>
            <div class="form-group col-md-6">
                @*<label asp-for="@Model.AppUser.CustLastName">Last Name</label>
                <input class="form-control" asp-for="@Model.AppUser.CustLastName" value="@Model.AppUser.CustLastName" />*@
            </div>
        </div>
        <div class="row">
            <div class="form-group col-md-6">
                @*<label asp-for="@Model.AppUser.Email">Email</label>
                <input class="form-control" asp-for="@Model.AppUser.Email" value="@Model.AppUser.Email" />*@
            </div>
        </div>
        <div class="row">
            <div class="form-group col-md-6">
                @*<label asp-for="@Model.AppUser.PhoneNumber">Phone Number</label>
                <input class="form-control" asp-for="@Model.AppUser.PhoneNumber" value="@Model.AppUser.PhoneNumber" />*@
            </div>
        </div>
        @{
            @*foreach (var r in Model.AllRoles)
            {
                <div class="form-check">
                    @{
                        if (Model.UsersRoles.Contains(r))
                        {
                            <input class="form-check-input" name="roles" type="checkbox" id="@r" value="@r" checked>
                            <label class="form-check-label">@r</label>
                        }
                        else
                        {
                            <input class="form-check-input" name="roles" type="checkbox" id="@r" value="@r">
                            <label class="form-check-label">@r</label>
                        }
                    }
                </div>
            }*@
        }
        <button type="submit">Update</button>
    </form>
</div>
